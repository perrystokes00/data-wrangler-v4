==============================================================================
dead code — C:\Users\perry\OneDrive\Documents\PPDM\claude_use_ai\data_wrangler\data_wrangler_clean\dataview\import_data
47 file(s) parsed
==============================================================================

1. UNREACHABLE  — proven: these statements cannot execute
------------------------------------------------------------------------------
  2 block(s). This is the finding to act on — the rest are questions.

  page_dir_loader.py:696
      unreachable — preceding statement is a st.rerun() never returns; 1 statement(s) after it
  page_pipeline.py:862
      unreachable — preceding statement is a st.rerun() never returns; 4 statement(s) after it

  A `st.rerun()` / `st.stop()` raises. Anything after it in the same block is
  dead and looks completely normal — no linter flags it.

2. NEVER CALLED  — defined here, name appears nowhere in this tree
------------------------------------------------------------------------------
  17 name(s). NOT proof. Verify each before deleting:
    · called from OUTSIDE this root? re-run with --root at the repo root
    · called by name from a .bat, a test, or SQL? not scanned
    · reached via getattr / a string this scan didn't recognise as an identifier?

  entity_seeder.py
      :98    seed_entities
  exporters.py
      :651   render_export_ui
      :1359  render_export_page
  load_router.py
      :129   _fmt
      :133   _listing
  mapping.py
      :573   save_entity_mapping
      :595   restore_entity_mapping
  page_dir_loader.py
      :497   _map_entity_fk_target
      :509   build_load_bundle
      :556   fk_coverage
      :687   _colmap_stage
      :769   _validate_stage
      :821   _fk_stage
  pipeline_run.py
      :1764  _engine_spec
  staging.py
      :896   load_from_path
  staging_repair.py
      :66    looks_mangled
      :243   iso_from

3. NEVER IMPORTED  — no module in this tree imports them
------------------------------------------------------------------------------
  entity_seeder, export_for_snowflake, load_diagnostics (3), load_router, page_ai_importer, page_bulk, page_fed_loader, page_import_gom, page_import_gom_dir_srvy, page_import_osdu, page_import_rrc, page_import_rrc_shp, page_import_shapefile, page_import_witsml, page_pipeline, page_pipeline_tools, pipeline_batch_ui, pipeline_proc_runner, pipeline_profiler, repromote, run_stage, upload_to_snowflake

  Weakest signal of the three. An entry point, a CLI script run by hand, a
  page dispatched by string, or anything a .bat launches will all appear here
  and be perfectly alive.

==============================================================================
Section 1 is proof. Sections 2 and 3 are questions — a name scan cannot see
dynamic dispatch, and this codebase dispatches on strings in at least two places.
==============================================================================
